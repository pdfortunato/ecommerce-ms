server:
  port: 8081

spring:
  application:
    name: user-service  # Nome che Eureka userà per il routing

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true